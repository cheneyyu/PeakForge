# Additional LOPO validation data

This directory is used for the extra K562 and HepG2 replicate pair required by `example/run_lopo_validation.sh`.

The files are not committed to git. Download them with:

```bash
DRY_RUN=0 INCLUDE_THIRD_REPLICATES=1 bash ../download_encode.sh
```
